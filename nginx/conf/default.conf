# TEMPORARILY DISABLED SSL SERVER BLOCK
# server {
#     listen 443 ssl;
#     server_name mykanplan.fr www.mykanplan.fr;
#
#     ssl_certificate /etc/letsencrypt/live/mykanplan.fr/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/mykanplan.fr/privkey.pem;
#
#     root /var/www/html/public;
#     index index.php index.html;
#
#     location / {
#         try_files $uri $uri/ /index.php?$query_string;
#     }
#
#     location ~ \.php$ {
#         fastcgi_read_timeout 300s;
#         fastcgi_send_timeout 300s;
#         fastcgi_connect_timeout 300s;
#         fastcgi_pass kanboard_app:9000;
#         include fastcgi_params;
#         fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
#     }
#     location ~ /\.ht {
#         deny all;
#     }
# }

# HTTP Server (now serving the application instead of redirecting)
server {
    listen 80;
    server_name localhost;

    root /var/www/html/public;
    index index.php index.html;

    location / {
        try_files $uri $uri/ /index.php?$query_string;
    }

    location ~ \.php$ {
        fastcgi_pass kanboard_app:9000;
        fastcgi_index index.php;
        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
    }
     location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
        access_log off;
        log_not_found off;
        try_files $uri =404;
    }
}
